# Python
.venv/
__pycache__/
*.pyc

# Runtime data (provided by Docker volumes / local state)
config/
downloads/

# Secrets / local env
.env

# Host-specific deploy overrides (reverse-proxy routing, domains)
docker-compose.override.yml

# Editor / harness local settings
.claude/

# Synology / macOS metadata
@eaDir/
*@SynoEAStream
.DS_Store
Design.png
